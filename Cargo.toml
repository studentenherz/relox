[package]
name = "relox"
version = "0.1.0"
edition = "2024"
authors = ["Michel Romero"]

[dependencies]
macros = { path = "./macros" }

[features]
debug = []
debug-trace-execution = ["debug"]
debug-print-code = ["debug"]
